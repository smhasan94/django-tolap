{
  "description": "Two policies bound to DIFFERENT purposes -- the merge fails closed to deny-all rather than picking one or dropping the profile. Resolution cannot produce this input (purpose filtering leaves a single purposeId), but merge() is public and must not depend on its caller having filtered first.",
  "inputs": [
    {
      "version": "1.0",
      "name": "overlap",
      "priority": 10,
      "permissions": { "canQuery": true, "readOnly": true },
      "purposeProfile": {
        "purposeId": "campaign-x-overlap",
        "allowedActions": ["aggregate_overlap"]
      }
    },
    {
      "version": "1.0",
      "name": "fraud",
      "priority": 20,
      "permissions": { "canQuery": true, "readOnly": true },
      "purposeProfile": {
        "purposeId": "fraud-detection",
        "allowedActions": ["inspect_account"]
      }
    }
  ],
  "expected": {
    "sourceProfiles": [],
    "permissions": { "canQuery": false, "readOnly": true }
  }
}
