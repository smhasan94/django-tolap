[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "sqlalchemy-tolap"
version = "0.2.1"
description = "TOLAP policies enforced on SQLAlchemy Select statements with ORM-native pushdown"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [{ name = "Sharukh Hasan" }]
keywords = ["sqlalchemy", "tolap", "access-control", "ai-agents", "mcp"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Database",
  "Topic :: Security",
]
dependencies = [
  "SQLAlchemy>=2.0,<2.2",
  "tolap-core>=1.0,<2",
]

[project.urls]
Homepage = "https://github.com/smhasan94/django-tolap"
Issues = "https://github.com/smhasan94/django-tolap/issues"
Changelog = "https://github.com/smhasan94/django-tolap/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["sqlalchemy_tolap"]
